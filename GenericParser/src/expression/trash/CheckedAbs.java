package expression.trash;

public final class CheckedAbs {

    /*public CheckedAbs(CommonExpression first) {
        super(first);
    }

    @Override
    protected int compute(int x) {
        if (x == Integer.MIN_VALUE) {
            throw new OverflowException();
        }
        if (x < 0) {
            return -x;
        } else {
            return x;
        }
    }

    @Override
    protected String getOperationType() {
        return "abs";
    }

    @Override
    public int getPriority() {
        return 5;
    }

    @Override
    public boolean isOrdered() {
        return false;
    }

    @Override
    protected double compute(double x) {
        if (x < 0) {
            return -x;
        } else {
            return x;
        }
    }*/
}